{
    "version": 2,
    "buildCommand": "prisma generate && nuxt build",
    "outputDirectory": ".output/public",
    "framework": "nuxtjs",
    "git": {
        "deploymentEnabled": {
            "dev": false
        }
    }
}